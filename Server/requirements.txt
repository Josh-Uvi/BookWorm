# Runtime dependencies — no cloud SDKs.
websockets>=13,<18
python-dotenv>=1.0
numpy>=1.26

# Speech-to-text (offline)
faster-whisper>=1.0

# Language model — OpenAI-compatible client (Ollama / vLLM / Groq / OpenAI ...)
openai>=1.40

# Text-to-speech
edge-tts>=6.1          # zero-setup, needs internet
piper-tts>=1.2         # fully offline (download voices via scripts/download_piper_voice.py)

# Optional S3-compatible storage backend (STORAGE_PROVIDER=minio)
minio>=7.2
